# Installation

1. Instale `loyalty` (Coupons & Loyalty).
2. Instale `l10n_ve_loyalty`.

Al actualizar `l10n_ve_seniat` desde una versión que ya incluía descuentos,
el pre-migrate marca `l10n_ve_loyalty` como `to install` y reasigna los
XML-IDs. Basta con `-u l10n_ve_seniat`.
