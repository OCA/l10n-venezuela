The localization is split across many addons. This module exists so we can detect inheritance and view conflicts after they are installed together.

Not pulled here: l10n_ve_bank_reconcile_ref (OCA reconcile excludes Invoicing in this stack), l10n_ve_reports and its dependents (reports XML needs account.report actions that fail to load).
