Add the configured payment method to the POS. When validating an order, enter the
exact VES amount from the payment receipt and, when configured, its transaction
reference. The current exchange rate supplies a suggested amount but never
replaces the amount confirmed by the cashier.

Do not declare a closing difference on a VES tender. Correct the individual POS
payment instead so the VES liquidity line continues to match the bank statement.
